# For demo purposes the frontend ships with a built-in mock API.
# To use the mock API:
VITE_MOCK_API=true

# To use a real backend set VITE_MOCK_API=false and provide the backend base URL:
# VITE_MOCK_API=false
# VITE_API_BASE=http://localhost:4000

# Optional: client-side Web3.Storage token for direct uploads
# VITE_WEB3_STORAGE_TOKEN=REPLACE_WITH_TOKEN
